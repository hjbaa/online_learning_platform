=render 'shared/errors', object: @test

=form_with model: [:teacher, @test], local: false do |f|
  =f.number_field :author_id, class: 'hidden'
  =f.number_field :article_id, class: 'hidden'
  =f.submit 'Create test for article', class: 'btn btn-outline-primary btn-lg mb-4'